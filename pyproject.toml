[project]
name = "ellai"
version = "0.1.0"
description = "Ellai is a personal assistant, powered by local LLMs."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "gradio>=6.9.0",
    "ollama>=0.6.1",
]
